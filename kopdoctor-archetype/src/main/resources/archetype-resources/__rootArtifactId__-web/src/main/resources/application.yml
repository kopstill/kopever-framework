spring:
  application:
    name: ${rootArtifactId}${KOPDOCTOR_DEVELOPER_NAME}
  cloud:
    consul:
      host: ${KOPDOCTOR_REGISTRY_ADDRESS}
      port: 8500
      discovery:
        service-name: ${rootArtifactId}
        hostname: ${rootArtifactId}